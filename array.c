#include <stdio.h>
#include<conio.h>

int main()
{
    int a[10],i,n,x=0;
    printf("enter array size:\n");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        printf("enter no :");
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a)
    }
    
    return 0;
}